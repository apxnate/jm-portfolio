# Use ONE of the following providers:

# Option A — OpenAI (GPT-4o-mini, recommended)
OPENAI_API_KEY=sk-...

# Option B — Groq (Llama-3.1-70B, faster & free tier available)
# GROQ_API_KEY=gsk_...

# The app auto-detects which key is set. GROQ_API_KEY takes priority if both are present.
